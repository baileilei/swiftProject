Git Exclude
Git Patch
git reset  git revert
在git push前如何遗弃掉历史commit-----------------------------------------practice
git 如何删除中间某个commit同时保留后面的commit
Git撤销&回滚操作





TODO:
1.pod SVProgessHUD    AFNetworking
2.日文？
3.研究contact Search那个控件。
4.先处理不用pod。 后期再说pod吧。

5.自定义一个patch  打印的，  某个文件打印的patch     开关个性化定制

6.定时在APP Store发布版本   ---强制更新。    也有不强制更新的方法。----通过接口！ 服务器内置。



